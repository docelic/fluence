# This configuration file was generated by `ameba --gen-config`
# on 2018-08-03 19:09:18 +02:00 using Ameba version 0.7.0.
# The point is for the user to remove these configuration records
# one by one as the reported problems are removed from the code base.

# Problems found: 9
# Run `ameba --only UselessAssign` for details
UselessAssign:
  Description: Disallows useless variable assignments
  Enabled: true
  Excluded:
  - src/lib/users/users.cr
  - src/controllers/helpers/page.cr
  - src/controllers/pages_controller.cr
  - src/controllers/admin_controller.cr

# Problems found: 2
# Run `ameba --only UnusedArgument` for details
UnusedArgument:
  Description: Disallows unused arguments
  IgnoreDefs: true
  IgnoreBlocks: false
  IgnoreProcs: false
  Enabled: true
  Excluded:
  - src/controllers/helpers/page.cr
