require "./page"
require "./sfile"

# TODO: Index handler, to build and parse index files that keep a list of files and meta-data
class Wikicr::Page::Index
  # getter dir : String
  # getter pages : Array(SFile)

  # def initialize(@dir)
  #   @pages = Dir.entries(@dir).map { |f| SFile.build(f) }
  # end

  # def build!
  # end
end
